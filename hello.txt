I can neither read nor write. 
I solemnly swear that I am up to no good.

git init //initialize the git project in the current folder
git add . // adds all to the staging area
git commit //commits the code 

git remote add origin https://github.com/Prodigalis/demonstration.git //this basically creates a link that can be used to push or pull the repos.
git remote -v // shows you the links to the remote repo

git config user.name" " // sets the name of the user doing the commmits
git config user.email " "// does the same for email.
git config user.name //returns the people using the repo
git config user.email // does the same for emails. 


git log// shows what has happened in the folder
git status // does something
git difference // shows the changes
